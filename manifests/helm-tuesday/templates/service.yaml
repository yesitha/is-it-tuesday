apiVersion: v1
kind: Service
metadata:
  name: {{ include "tuesday.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "tuesday.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: flask
  selector:
    {{- include "tuesday.selectorLabels" . | nindent 4 }}
