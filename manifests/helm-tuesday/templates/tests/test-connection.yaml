apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tuesday.fullname" . }}-test-connection"
  labels:
    {{- include "tuesday.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tuesday.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
